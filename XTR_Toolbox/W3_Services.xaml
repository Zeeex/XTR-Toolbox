<Window x:Class="XTR_Toolbox.Window3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Batch Services Manager (Context menu to manage)" Height="561.5" Width="845"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        WindowStartupLocation="CenterScreen" FontSize="14">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ListView x:Name="LvServices" Margin="0" Grid.Row="1">
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem x:Name="Start" Header="Sta_rt Service" Click="MenuItem_Click" />
                    <MenuItem x:Name="Stop" Header="_Stop Service" Click="MenuItem_Click" />
                    <Separator />
                    <MenuItem x:Name="Disable" Header="_Disable" Click="MenuItem_Click" />
                    <MenuItem x:Name="Manual" Header="_Manual" Click="MenuItem_Click" />
                    <MenuItem x:Name="Auto" Header="_Automatic" Click="MenuItem_Click" />
                    <MenuItem x:Name="AutoDelayed" Header="Aut_o (Delayed)" Click="MenuItem_Click" />
                </ContextMenu>
            </ListView.ContextMenu>
            <ListView.View>
                <GridView AllowsColumnReorder="False">
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Width="200">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Name" Click="LvUsersColumnHeader_Click">Service Name</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Full}" Width="350">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Full" Click="LvUsersColumnHeader_Click">Full Name</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Status}">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Status" Click="LvUsersColumnHeader_Click">Status</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Startup}" Width="150">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Startup" Click="LvUsersColumnHeader_Click">Startup Type</GridViewColumnHeader>
                        </GridViewColumn.Header>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <TextBox x:Name="TxtSearch" Grid.Row="0" Height="24" Margin="185,6,10,0" TextWrapping="Wrap" TabIndex="0"
                 FontSize="14" TextChanged="TxtSearch_TextChanged" AcceptsTab="True" VerticalAlignment="Top" />
        <Label Grid.Row="0" Content="Quick &quot;Full Name&quot; Search:" HorizontalAlignment="Left" Margin="10,7,0,0"
               VerticalAlignment="Top" FontSize="14" />
    </Grid>
    <Window.CommandBindings>
        <CommandBinding Command="Close" Executed="OnCloseExecuted" />
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Command="Close" Key="Escape" />
    </Window.InputBindings>
</Window>