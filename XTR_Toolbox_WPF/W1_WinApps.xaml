<Window x:Class="XTR_Toolbox.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Manage Windows Apps" Height="515" Width="555"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="33" />
            <RowDefinition />
            <RowDefinition Height="55" />
        </Grid.RowDefinitions>
        <CheckBox Content="Check All Items" Checked="ChBoxAll_Checked" Unchecked="ChBoxAll_Unchecked"
                  FontSize="14" Margin="10,0"
                  Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="0" />
        <ListBox x:Name="AppsListBox" Margin="8,0" Grid.ColumnSpan="2" Grid.Column="0" FontSize="14" Grid.Row="1"
                 SelectionChanged="AppsListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Text}" IsChecked="{Binding Checked}"
                              d:DataContext="{d:DesignData }" IsTabStop="False" />
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button x:Name="BtnDelApps" Content="Uninstall Selected Apps" Grid.Row="2" Grid.Column="0"
                FontSize="14" FontWeight="Bold" Click="BtnProcessApps_Click" Margin="10,10,5,10" Height="Auto" />
        <Button x:Name="BtnRepApps" Content="Reinstall All Apps" Grid.Row="2" Grid.Column="1"
                FontSize="14" FontWeight="Bold" Click="BtnProcessApps_Click" Margin="5,10,10,10" Height="Auto" />
    </Grid>
    <Window.CommandBindings>
        <CommandBinding Command="Close" Executed="OnCloseExecuted" />
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Command="Close" Key="Escape" />
    </Window.InputBindings>
</Window>